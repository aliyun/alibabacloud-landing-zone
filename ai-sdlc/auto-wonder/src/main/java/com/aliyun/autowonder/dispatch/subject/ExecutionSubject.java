package com.aliyun.autowonder.dispatch.subject;

/** Loaded, workspace-validated execution root. Domain providers keep its payload private. */
public interface ExecutionSubject {
    ExecutionSubjectRef ref();
}

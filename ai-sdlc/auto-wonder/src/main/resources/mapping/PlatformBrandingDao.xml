<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aliyun.autowonder.branding.PlatformBrandingDao">

    <select id="findActive" resultType="com.aliyun.autowonder.branding.PlatformBrandingDO">
        SELECT *
        FROM platform_branding_config
        WHERE id = 1 AND is_deleted = 0
        LIMIT 1
    </select>

    <update id="update" parameterType="com.aliyun.autowonder.branding.PlatformBrandingDO">
        UPDATE platform_branding_config
        SET platform_name = #{platformName},
            theme_key = #{themeKey},
            primary_color = #{primaryColor},
            domain = #{domain},
            modifier_id = #{modifierId},
            version = version + 1
        WHERE id = 1 AND is_deleted = 0
    </update>

    <update id="updateLogo">
        UPDATE platform_branding_config
        SET logo_oss_ref = #{logoOssRef},
            logo_content_type = #{logoContentType},
            modifier_id = #{modifierId},
            version = version + 1
        WHERE id = 1 AND is_deleted = 0
    </update>
</mapper>

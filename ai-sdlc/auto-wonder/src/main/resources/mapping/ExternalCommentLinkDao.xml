<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aliyun.autowonder.integration.common.ExternalCommentLinkDao">
    <insert id="insert" parameterType="com.aliyun.autowonder.integration.common.ExternalCommentLinkDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO external_comment_link (tenant_id, provider, binding_id, external_workitem_id,
            external_comment_id, workitem_comment_id, direction)
        VALUES (#{tenantId}, #{provider}, #{bindingId}, #{externalWorkitemId},
            #{externalCommentId}, #{workitemCommentId}, #{direction})
    </insert>
    <select id="findByExternal" resultType="com.aliyun.autowonder.integration.common.ExternalCommentLinkDO">
        SELECT * FROM external_comment_link
        WHERE tenant_id = #{tenantId} AND provider = #{provider} AND external_comment_id = #{externalCommentId}
        LIMIT 1
    </select>
    <select id="findByLocalComment" resultType="com.aliyun.autowonder.integration.common.ExternalCommentLinkDO">
        SELECT * FROM external_comment_link
        WHERE tenant_id = #{tenantId} AND workitem_comment_id = #{workitemCommentId}
        LIMIT 1
    </select>
</mapper>

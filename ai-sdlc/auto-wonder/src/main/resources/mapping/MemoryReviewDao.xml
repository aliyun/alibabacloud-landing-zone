<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aliyun.autowonder.memory.MemoryReviewDao">
    <insert id="insert" parameterType="com.aliyun.autowonder.memory.MemoryReviewDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO memory_review (tenant_id, memory_id, reviewer_id, decision, edited_content_md, comment)
        VALUES (#{tenantId}, #{memoryId}, #{reviewerId}, #{decision}, #{editedContentMd}, #{comment})
    </insert>
    <select id="listByMemoryId" resultType="com.aliyun.autowonder.memory.MemoryReviewDO">
        SELECT * FROM memory_review WHERE memory_id = #{memoryId} ORDER BY gmt_create DESC
    </select>
</mapper>

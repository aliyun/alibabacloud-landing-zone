<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aliyun.autowonder.aiusage.DispatchAiUsageDao">
    <insert id="upsert" parameterType="com.aliyun.autowonder.aiusage.DispatchAiUsageDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dispatch_ai_usage
            (tenant_id, workitem_id, dispatch_id, agent_id, executor_id, artifact_id,
             provider, model, input_tokens, output_tokens, cache_read_tokens, cache_write_tokens,
             total_tokens, raw_json, usage_at)
        VALUES
            (#{tenantId}, #{workitemId}, #{dispatchId}, #{agentId}, #{executorId}, #{artifactId},
             #{provider}, #{model}, #{inputTokens}, #{outputTokens}, #{cacheReadTokens}, #{cacheWriteTokens},
             #{totalTokens}, #{rawJson}, #{usageAt})
        ON DUPLICATE KEY UPDATE
            id = LAST_INSERT_ID(id),
            workitem_id = VALUES(workitem_id),
            agent_id = VALUES(agent_id),
            executor_id = COALESCE(VALUES(executor_id), executor_id),
            artifact_id = COALESCE(VALUES(artifact_id), artifact_id),
            input_tokens = VALUES(input_tokens),
            output_tokens = VALUES(output_tokens),
            cache_read_tokens = VALUES(cache_read_tokens),
            cache_write_tokens = VALUES(cache_write_tokens),
            total_tokens = VALUES(total_tokens),
            raw_json = VALUES(raw_json),
            usage_at = VALUES(usage_at),
            gmt_modified = NOW(3)
    </insert>

    <update id="attachArtifactIfAbsent">
        UPDATE dispatch_ai_usage
        SET artifact_id = #{artifactId}, gmt_modified = NOW(3)
        WHERE tenant_id = #{tenantId}
          AND dispatch_id = #{dispatchId}
          AND provider = #{provider}
          AND model = #{model}
          AND artifact_id IS NULL
    </update>
</mapper>

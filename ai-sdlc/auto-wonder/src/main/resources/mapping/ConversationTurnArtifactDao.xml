<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aliyun.autowonder.conversation.ConversationTurnArtifactDao">

    <resultMap id="BaseResultMap" type="com.aliyun.autowonder.conversation.ConversationTurnArtifactDO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="conversation_id" property="conversationId"/>
        <result column="turn_id" property="turnId"/>
        <result column="artifact_id" property="artifactId"/>
        <result column="direction" property="direction"/>
        <result column="reference_mode" property="referenceMode"/>
        <result column="display_order" property="displayOrder"/>
        <result column="manifest_json" property="manifestJson"/>
        <result column="gmt_create" property="gmtCreate"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, tenant_id, conversation_id, turn_id, artifact_id, direction, reference_mode,
        display_order, manifest_json, gmt_create
    </sql>

    <insert id="insert" parameterType="com.aliyun.autowonder.conversation.ConversationTurnArtifactDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO conversation_turn_artifact
        (tenant_id, conversation_id, turn_id, artifact_id, direction, reference_mode, display_order, manifest_json)
        VALUES
        (#{tenantId}, #{conversationId}, #{turnId}, #{artifactId}, #{direction}, #{referenceMode},
         #{displayOrder}, #{manifestJson})
    </insert>

    <select id="listByTurn" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM conversation_turn_artifact
        WHERE tenant_id = #{tenantId} AND turn_id = #{turnId}
        ORDER BY direction ASC, display_order ASC, id ASC
    </select>

    <select id="listByConversation" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM conversation_turn_artifact
        WHERE tenant_id = #{tenantId} AND conversation_id = #{conversationId}
        ORDER BY turn_id DESC, direction ASC, display_order ASC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>

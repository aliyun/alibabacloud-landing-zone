<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aliyun.autowonder.workitem.WorkitemCommentDao">
    <sql id="cols" databaseId="autowonder-source-aware">
        id, tenant_id, source_type, workitem_id, author_type, author_ref, content_md, gmt_create
    </sql>
    <sql id="cols" databaseId="autowonder-legacy">
        id, tenant_id, 'WORKITEM' AS source_type, workitem_id, author_type, author_ref, content_md, gmt_create
    </sql>

    <insert id="insert" parameterType="com.aliyun.autowonder.workitem.WorkitemCommentDO"
            databaseId="autowonder-source-aware"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO workitem_comment
            (tenant_id, source_type, workitem_id, author_type, author_ref, content_md, gmt_create)
        VALUES
            (#{tenantId}, #{sourceType}, #{workitemId}, #{authorType}, #{authorRef}, #{contentMd},
             COALESCE(#{gmtCreate}, CURRENT_TIMESTAMP(3)))
    </insert>
    <insert id="insert" parameterType="com.aliyun.autowonder.workitem.WorkitemCommentDO"
            databaseId="autowonder-legacy" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO workitem_comment (tenant_id, workitem_id, author_type, author_ref, content_md, gmt_create)
        VALUES (#{tenantId}, #{workitemId}, #{authorType}, #{authorRef}, #{contentMd},
                COALESCE(#{gmtCreate}, CURRENT_TIMESTAMP(3)))
    </insert>
    <select id="findById" databaseId="autowonder-source-aware"
            resultType="com.aliyun.autowonder.workitem.WorkitemCommentDO">
        SELECT <include refid="cols"/> FROM workitem_comment
        WHERE tenant_id = #{tenantId} AND source_type = 'WORKITEM' AND id = #{id} LIMIT 1
    </select>
    <select id="findById" databaseId="autowonder-legacy"
            resultType="com.aliyun.autowonder.workitem.WorkitemCommentDO">
        SELECT <include refid="cols"/> FROM workitem_comment
        WHERE tenant_id = #{tenantId} AND id = #{id} LIMIT 1
    </select>
    <select id="listByWorkitem" databaseId="autowonder-source-aware"
            resultType="com.aliyun.autowonder.workitem.WorkitemCommentDO">
        SELECT <include refid="cols"/> FROM workitem_comment WHERE tenant_id = #{tenantId}
          AND source_type = 'WORKITEM' AND workitem_id = #{workitemId}
        ORDER BY gmt_create DESC, id DESC
    </select>
    <select id="listByWorkitem" databaseId="autowonder-legacy"
            resultType="com.aliyun.autowonder.workitem.WorkitemCommentDO">
        SELECT <include refid="cols"/> FROM workitem_comment WHERE tenant_id = #{tenantId}
          AND workitem_id = #{workitemId}
        ORDER BY gmt_create DESC, id DESC
    </select>
    <select id="findBySourceAndId" databaseId="autowonder-source-aware"
            resultType="com.aliyun.autowonder.workitem.WorkitemCommentDO">
        SELECT <include refid="cols"/> FROM workitem_comment WHERE tenant_id = #{tenantId} AND source_type = #{sourceType}
          AND workitem_id = #{sourceId} AND id = #{id} LIMIT 1
    </select>
    <select id="listBySource" databaseId="autowonder-source-aware"
            resultType="com.aliyun.autowonder.workitem.WorkitemCommentDO">
        SELECT <include refid="cols"/> FROM workitem_comment WHERE tenant_id = #{tenantId} AND source_type = #{sourceType}
          AND workitem_id = #{sourceId} ORDER BY id ASC
    </select>
    <update id="updateExternalContent" databaseId="autowonder-source-aware">
        UPDATE workitem_comment
        SET author_ref = COALESCE(#{authorRef}, author_ref),
            content_md = COALESCE(#{contentMd}, content_md)
        WHERE tenant_id = #{tenantId} AND id = #{id}
          AND source_type = 'WORKITEM' AND author_type = 'EXTERNAL'
    </update>
    <update id="updateExternalContent">
        UPDATE workitem_comment
        SET author_ref = COALESCE(#{authorRef}, author_ref),
            content_md = COALESCE(#{contentMd}, content_md)
        WHERE tenant_id = #{tenantId} AND id = #{id} AND author_type = 'EXTERNAL'
    </update>
</mapper>
